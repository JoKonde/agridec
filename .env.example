# Variables d'environnement — copiez ce fichier en `.env` et adaptez les valeurs.
# Ne commitez jamais le fichier `.env` (secrets).

# Django
DJANGO_SECRET_KEY=changez-moi-en-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL
DB_NAME=agridec
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
